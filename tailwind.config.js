// Root proxy — Tailwind/NativeWind discover the config at the project root,
// but the canonical source lives under styles/ per PRD §4.1 folder layout.
export { default } from './styles/tailwind.config.js';

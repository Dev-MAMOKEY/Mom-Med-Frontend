import { Text, View } from 'react-native';

// 알레르기 추가 모달 placeholder — 다음 커밋에서 타입 칩·추천·severity·메모 폼으로 교체
export default function AllergyAdd() {
  return (
    <View className="flex-1 items-center justify-center bg-bg px-6">
      <Text className="text-base text-text-soft text-center">
        알레르기 추가 (구현 중)
      </Text>
    </View>
  );
}
